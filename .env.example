# Copy to .env for local non-Docker runs only.
# Docker Compose injects these automatically.

DATABASE_URL=postgres://ajgym:ajgym_secret@localhost:5432/aj_gym
PORT=3001
NODE_ENV=development

# Browser (Vite) — leave empty in Docker; nginx proxies /api and /ws.
VITE_API_BASE=
